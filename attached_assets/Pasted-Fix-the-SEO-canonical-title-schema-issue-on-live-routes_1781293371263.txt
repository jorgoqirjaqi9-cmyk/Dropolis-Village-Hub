Fix the SEO canonical/title/schema issue on live routes. Current problem: frontend artifact has a production rewrite /* -> /index.html, so routes like /news, /villages, /news/:id serve the generic root HTML with canonical https://dropolis.net/ and no NewsArticle schema. Keep SPA refresh working, but make crawlers receive route-specific HTML.

Implement a robust server-side HTML meta route in the API artifact and route public page paths to it.

Required changes:

1) Add an API root-level route module, e.g. artifacts/api-server/src/routes/seo-pages.ts or lib/seo-pages.ts, mounted in artifacts/api-server/src/app.ts BEFORE /api.

It must handle GET requests for public page routes:
- /news
- /news/:id
- /villages
- /villages/:id
- /photos
- /videos
- /about
- /contact
- /press
- /help
- /privacy
- /terms
- /cookie-policy
- /disclaimer
- /sitemap
- /chat

For those routes, return full HTML based on the frontend built template (artifacts/dropolis/dist/public/index.html in production, and artifacts/dropolis/index.html fallback in dev if needed). Inject/replace SEO tags in <head> and inject a body fallback inside <div id="root"> with exactly one h1 + paragraph.

2) For article pages /news/:id:
- Query articlesTable by id AND published=true.
- If missing/unpublished, return 404.
- Use per-article meta:
  title = article.seoTitle || article.title
  description = article.metaDescription || article.excerpt || cleaned article.content slice
  canonical = https://dropolis.net/news/{id}
  og:type = article
  og:image = article.imageUrl fallback /opengraph.jpg
  article:published_time, article:modified_time, article:author, article:section
  JSON-LD NewsArticle with headline, description, image, datePublished, dateModified, author, publisher, mainEntityOfPage, articleSection, inLanguage el
  BreadcrumbList: Αρχική > Ειδήσεις > article title
  Body fallback h1 should be article.title.

3) For village pages /villages/:id:
- Query villagesTable by id.
- If missing, return 404.
- canonical = https://dropolis.net/villages/{id}
- title = village.nameEl + " — Χωριό της Δρόπολης"
- description from village.description or useful fallback
- JSON-LD City with geo if available and BreadcrumbList.
- Body fallback h1 should be village.nameEl.

4) For static routes, use route-specific metadata, not the root metadata. It can either import/share from frontend route-manifest if build allows, or define a small API-side map. Must produce distinct canonical URLs:
/news -> https://dropolis.net/news
/villages -> https://dropolis.net/villages
/chat -> https://dropolis.net/chat
/privacy -> https://dropolis.net/privacy
etc.

5) Update artifacts/api-server/.replit-artifact/artifact.toml paths so the API server receives those public page routes in production, in addition to /api and legacy redirects. Example paths should include /news, /villages, /photos, /videos, /about, /contact, /press, /help, /privacy, /terms, /cookie-policy, /disclaimer, /sitemap, /chat, /privacy-policy, /terms-of-service. Do not route /assets, /favicon, /robots.txt, /sitemap.xml static files through the API.

6) Important HTML injection rules:
- Remove/replace existing <title>, meta description, canonical, og:title, og:description, og:type, og:url, og:image*, twitter:* and existing application/ld+json generated by the static template before injecting the route-specific tags.
- Keep AdSense, Google Analytics, scripts, styles and Vite asset links untouched.
- Fix index.html if it still has accidental corruption like <dipwd or malformed </div">; it must be valid.
- Do not redesign UI.
- Do not reintroduce chat bot.

7) Run typecheck/build. Fix any TS errors. Restart server. Republish/deploy.

After deploy, verify with curl/requests:
- https://dropolis.net/news has canonical https://dropolis.net/news and title not equal to homepage title.
- https://dropolis.net/villages has canonical https://dropolis.net/villages.
- latest article /news/{id} has canonical /news/{id}, contains NewsArticle JSON-LD, and title includes article title.
- homepage still works and /api/articles still works.